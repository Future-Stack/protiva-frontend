services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    image: sakibsakib/protiva:latest
    platform: linux/amd64
    environment:
      NODE_ENV: development
    env_file:
      - .env
    ports:
      - "3000:3000"
